{
  "name": "@rpallas/example-account-baseline",
  "version": "0.0.0",
  "private": true,
  "description": "Reference account baseline app using @rpallas/outcrop-account.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "test": "jest",
    "synth": "cdk synth -c env=dev",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@rpallas/outcrop-account": "0.1.0"
  }
}
